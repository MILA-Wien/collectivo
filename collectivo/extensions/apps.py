"""App configuration of the extensions app."""
from django.apps import AppConfig


class ExtensionsConfig(AppConfig):
    """App configuration of the extensions app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'extensions'
